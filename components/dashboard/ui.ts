// Shared dashboard typography.
//
// The label above a figure or a panel. Small, tracked and uppercase: the
// convention corporate reporting uses to separate what a number is from what
// it says, and the cheapest way to make a screen read as an instrument rather
// than a feed.
//
// It lives here rather than being retyped per page so the dashboard cannot
// drift back into a dozen slightly different label styles.
export const LABEL =
  'text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-foreground'
